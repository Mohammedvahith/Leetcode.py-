class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        b = len(nums)
        for i in range(b):
            for j in range(1,b):
                if (i != j) and ((nums[i]+nums[j]) == target):
                    a = [i,j]   
        return a   
        
